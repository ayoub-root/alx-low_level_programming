#!/bin/bash

# Check if $CFILE variable is set
if [ -z "$CFILE" ]; then
  echo "Error: \$CFILE variable is not set."
  exit 1
fi

# Run the preprocessor and save the output to a file named 'c'
cpp "$CFILE" > c

echo "Preprocessing of $CFILE is complete. Result saved in 'c'."

